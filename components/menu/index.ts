import { css, html, LitElement } from 'lit'
import { customElement } from 'lit/decorators.js'

@customElement('x-menu')
export class Menu extends LitElement {
  static styles = css``

  render() {
    return html` <div>u-index component was created!!</div> `
  }
}
